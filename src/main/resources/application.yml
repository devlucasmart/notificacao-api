spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

rabbitmq:
  queue:
    proposta:
      pendente: proposta-pendente.ms-notificacao
      concluida: proposta-concluida.ms-notificacao

aws:
  acessKey: ${ACESS_KEY}
  secretKey: ${SECRET_KEY}

